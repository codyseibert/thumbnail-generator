## Getting Started

Create a free planet scale account, setup shadow branch, create password, copy both connection urls.

1. `npm install`
2. Create `.env` my copying .env.example to .env
4. Setup connection URLs
5. Initialize database `npx prisma migrate dev`
6. Run server `npm run dev`
